# To-do-list
* **26/06/23 제작 시작**
* Header 영역 HTML 구성완료
* **26/06/24 기록**
* `Main` HTML 3행 작성
1. 날짜~할일 목록 리스트
2. 글쓰기 버튼
3. 글쓰기 버튼 클릭 시 실행화면(팝업박스)
## 추후 동적 업데이트 예정 목록
* 날짜 터치(클릭) 시 **날짜 변경 가능** -> .date
* 등록된 할일 목록 터치(클릭) 시 완료 또는 **수정, 삭제 기능** -> .contents
* 글쓰기 CTA(화면에 고정된 버튼 제작) 버튼 클릭 시 글쓰기 팝업 실행되는 기능 
* 글쓰기 팝업 내에 원하는 할일 목록 입력되는 기능 .notice
* 글쓰기 팝업 내에 **글쓰기버튼 클릭 시 할일 목록으로 추가되는 기능** register_btn
* 입력 팝업창 바깥쪽 클릭 시 팝업창 사라지면서 main화면으로 나가지는 기능
* main에 있는 상단 **아이콘 터치 시 기능 추가 (search -> 기록 검색, menu -> 메뉴 출력)** .search_icon_btn, .menu_icon_btn
* add a new task 글자 or 입력창 내부 빈공간 터치 시 안내글 사라지면서 입력커서 실행
* 할일 목록 추가 시 main 화면에 있는 all옆에 숫자 새로 추가한 만큼 + and all 부분 클릭 시 전체 기록 사용자에게 출력
* main 화면에 today 클릭 시 현재 날짜에 기록된 할일 보여주기
* ToDo 제목 클릭시 메인 홈 .main_title

## CSS 작성 시 주의사항 및 기본순서
* CSS로 디자인 적용 전 html태그의 고유디자인이 활성화되어있으면 안된다.
*(위)가 활성화되어있다면 반드시 CSS명령어를 통해서 html디자인을 초기화 해야한다.
* chlrlghk(reset.css) 연결 후 `color, font-family`
정보 제작하는 디자인 특징에 맞게 재체크하기
* 초기화(reset.css) 연결 시 기존 디자인하는 다른 css파일보다 반드시 먼저 연결하기(순서상 가장 위로)
* 디자인css 진행 시 디자인하려는 html파일의 body태그의 첫번재 자식 태그부터 선택자를 만들고 순서대로 진행하기
* 특정 대상에 width, height 크기관련속성 작성 시 영역의 크기와 범위, 위치를 구분하기 위해 backround-color를 설정하기(실제 디자인 배경색이랑 연관없이 구분위한 테스트용으로 주로 영문명 사용)

# padding, border, margin
<!-- 테두리 적용 문번 border: 두께 모양 색상; --> 
* border: 1px solid black;
<!-- 안쪽 여백, 피그마 오토레이아웃 패딩과 동일 --> 
<!-- 안쪽 여백 패딩을 4방향 각각 넢는 법 --> 
# 방법1
padding-left: 15px;
padding-right: 15px;
padding-top: 45px;
padding-bottom: 45px; 
# 방법2
padding: 45px 15px; 상/하 , 좌/우 
# 방법3
padding: 45px 15px 45px; 상, 좌/우 ,하 
# 방법4
padding: 45px 15px 45px 15px; 상, 우, 하, 좌  
# 전체 여백값 동일시 패딩 값 1개 작성 -> 전체 방향 적용

# 글꼴 두께 -> regular 400 -> +/- 100씩 

# reset.css의 초기화 설정값에 따라 작성 유무 결정하기(적용값과 초기값이 같으면 생략)
 